// This file is a duplicate of dashboard/page.tsx and can be removed.
// The primary dashboard is now at /admin/dashboard
import AdminDashboard from "./dashboard/page";

export default function AdminRootPage() {
    return <AdminDashboard />;
}
